name: api-solid
services:
  pg:
   image: bitnami/postgresql:13.16.0
   ports:
     - '5432:5432'
   environment:
    - POSTGRES_USER=apiSolid
    - POSTGRES_PASSWORD=apiSolid
    - POSTGRES_DB=apiSolid
